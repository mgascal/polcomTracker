<%= form_for(@mainform) do |f| %>
  <% if @mainform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mainform.errors.count, "error") %> prohibited this mainform from being saved:</h2>

      <ul>
      <% @mainform.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
